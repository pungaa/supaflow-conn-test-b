# supaflow-conn-test-b

Public test artifact for the Supaflow GitHub source connector.
The repository's contents are generated by an automated seeding
script to produce a realistic-looking event stream (pushes,
pull requests, issues) for the connector's live integration
tests. Safe to delete at any time.

Companion repo: `pungaa/supaflow-conn-test-a`.
